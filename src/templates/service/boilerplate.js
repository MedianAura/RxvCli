define(["underscore", "rxvClass", "thenBy", "latinise", "rxvGlobal"],
	function (_, RxvClass, firstBy) {
		"use strict";
		
		var TestService = RxvClass.Model.extend({
		
		});
		
		return new TestService();
	});
